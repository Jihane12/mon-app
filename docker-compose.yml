version: "3.8"

networks:
  cicd-network:
    driver: bridge

services:
  app:
    build: .
    ports:
      - "8000:8000"
    networks:
      - cicd-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
